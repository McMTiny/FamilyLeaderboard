[build]
  publish = "."
  command = ""

[[headers]]
  for = "/*"
  [headers.values]
    Cache-Control = "public, max-age=0, must-revalidate"

# Ensures direct visits and PWA "Add to Home Screen" always land on the app
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
